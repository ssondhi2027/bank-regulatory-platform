# Copy to .env and fill in values locally. Never commit .env.
# More variables will be added as later phases (BigQuery load, dbt profiles) are built.

# GCP / BigQuery
GCP_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=
RAW_DATABASE=
BQ_LOCATION=
